	<%= periodically_call_remote(
	 :url => {:action => "version", :id => @movie.id},
	 :frequency => '5',
   :complete => "movie_update_handler(#{@movie.version}, request);") %>

<p>
  <b>Title:</b>
  <%=h @movie.title %>
</p>

<p>
  <b>Watched:</b>
  <%=h @movie.watched %>
</p>

<p>
  <b>Version:</b>
  <%=h @movie.version %>
</p>


<%= link_to 'Edit', edit_movie_path(@movie) %> |
<%= link_to 'Back', movies_path %>